name: first workflow

on: push

jobs:
 job1:
  runs-on: ubuntu-latest
  steps:
   - run: echo 'Hello for ubuntu'
 job2:
  needs: job1
  runs-on: windows-latest
  steps:
   - run: echo 'Hello for Windows'
